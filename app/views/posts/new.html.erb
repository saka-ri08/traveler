<h1>新規旅行投稿</h1> # 見出し
<%= form_with model: @post, local:true do |f| %> # フォーム
    <h4>旅行先</h4>
    <%= f.text_field :location %> # 1行の入力フォーム
    <h4>感想</h4>
    <%= f.text_area :text %> # 複数行の入力フォーム
    <f.submit '投稿する' %> # 送信ボタン
<% end %>

